<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.hotel.dao.MemberTypeDao">

    <resultMap type="com.hotel.entity.MemberType" id="MemberTypeMap">
        <result property="memTypeId" column="mem_type_Id" jdbcType="INTEGER"/>
        <result property="memtype" column="memType" jdbcType="VARCHAR"/>
        <result property="discount" column="discount" jdbcType="NUMERIC"/>
        <result property="remark" column="remark" jdbcType="VARCHAR"/>
    </resultMap>

    <!--查询单个-->
    <select id="queryById" resultMap="MemberTypeMap">
        select mem_type_Id,
               memType,
               discount,
               remark
        from hotelmanager.member_type
        where mem_type_Id = #{memTypeId}
    </select>

    <!--查询指定行数据-->
    <select id="queryAllByLimit" resultMap="MemberTypeMap">
        select mem_type_Id,
               memType,
               discount,
               remark
        from hotelmanager.member_type
        limit #{offset}, #{limit}
    </select>

    <!--通过实体作为筛选条件查询-->
    <select id="queryAll" resultMap="MemberTypeMap">
        select
        mem_type_Id, memType, discount, remark
        from hotelmanager.member_type
        <where>
            <if test="memTypeId != null">
                and mem_type_Id = #{memTypeId}
            </if>
            <if test="memtype != null and memtype != ''">
                and memType = #{memtype}
            </if>
            <if test="discount != null">
                and discount = #{discount}
            </if>
            <if test="remark != null and remark != ''">
                and remark = #{remark}
            </if>
        </where>
    </select>

    <!--新增所有列-->
    <insert id="insert" keyProperty="memTypeId" useGeneratedKeys="true">
        insert into hotelmanager.member_type(memType, discount, remark)
        values (#{memtype}, #{discount}, #{remark})
    </insert>

    <insert id="insertBatch" keyProperty="memTypeId" useGeneratedKeys="true">
        insert into hotelmanager.member_type(memType, discount, remark)
        values
        <foreach collection="entities" item="entity" separator=",">
            (#{entity.memtype}, #{entity.discount}, #{entity.remark})
        </foreach>
    </insert>

    <insert id="insertOrUpdateBatch" keyProperty="memTypeId" useGeneratedKeys="true">
        insert into hotelmanager.member_type(memType, discount, remark)
        values
        <foreach collection="entities" item="entity" separator=",">
            (#{entity.memtype}, #{entity.discount}, #{entity.remark})
        </foreach>
        on duplicate key update
        memType = values(memType) , discount = values(discount) , remark = values(remark)
    </insert>

    <!--通过主键修改数据-->
    <update id="update">
        update hotelmanager.member_type
        <set>
            <if test="memtype != null and memtype != ''">
                memType = #{memtype},
            </if>
            <if test="discount != null">
                discount = #{discount},
            </if>
            <if test="remark != null and remark != ''">
                remark = #{remark},
            </if>
        </set>
        where mem_type_Id = #{memTypeId}
    </update>

    <!--通过主键删除-->
    <delete id="deleteById">
        delete
        from hotelmanager.member_type
        where mem_type_Id = #{memTypeId}
    </delete>

</mapper>

