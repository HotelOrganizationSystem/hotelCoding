<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.hotel.dao.MemberDao">

    <resultMap type="com.hotel.entity.Member" id="MemberMap">
        <result property="memid" column="memId" jdbcType="INTEGER"/>
        <result property="usersid" column="usersId" jdbcType="INTEGER"/>
        <result property="memTypeId" column="mem_type_Id" jdbcType="INTEGER"/>
        <result property="memname" column="memName" jdbcType="VARCHAR"/>
        <result property="phone" column="phone" jdbcType="VARCHAR"/>
        <result property="memsex" column="memsex" jdbcType="VARCHAR"/>
        <result property="idcard" column="IDCard" jdbcType="VARCHAR"/>
        <result property="email" column="email" jdbcType="VARCHAR"/>
        <result property="memint" column="memInt" jdbcType="INTEGER"/>
        <result property="regisdate" column="regisdate" jdbcType="TIMESTAMP"/>
        <result property="userstate" column="userstate" jdbcType="VARCHAR"/>
        <result property="remark" column="remark" jdbcType="VARCHAR"/>
    </resultMap>

    <!--查询单个-->
    <select id="queryById" resultMap="MemberMap">
        select memId,
               usersId,
               mem_type_Id,
               memName,
               phone,
               memsex,
               IDCard,
               email,
               memInt,
               regisdate,
               userstate,
               remark
        from hotelmanager.member
        where memId = #{memid}
    </select>

    <!--查询指定行数据-->
    <select id="queryAllByLimit" resultMap="MemberMap">
        select memId,
               usersId,
               mem_type_Id,
               memName,
               phone,
               memsex,
               IDCard,
               email,
               memInt,
               regisdate,
               userstate,
               remark
        from hotelmanager.member
        limit #{offset}, #{limit}
    </select>

    <!--通过实体作为筛选条件查询-->
    <select id="queryAll" resultMap="MemberMap">
        select
        memId, usersId, mem_type_Id, memName, phone, memsex, IDCard, email, memInt, regisdate, userstate, remark
        from hotelmanager.member
        <where>
            <if test="memid != null">
                and memId = #{memid}
            </if>
            <if test="usersid != null">
                and usersId = #{usersid}
            </if>
            <if test="memTypeId != null">
                and mem_type_Id = #{memTypeId}
            </if>
            <if test="memname != null and memname != ''">
                and memName = #{memname}
            </if>
            <if test="phone != null and phone != ''">
                and phone = #{phone}
            </if>
            <if test="memsex != null and memsex != ''">
                and memsex = #{memsex}
            </if>
            <if test="idcard != null and idcard != ''">
                and IDCard = #{idcard}
            </if>
            <if test="email != null and email != ''">
                and email = #{email}
            </if>
            <if test="memint != null">
                and memInt = #{memint}
            </if>
            <if test="regisdate != null">
                and regisdate = #{regisdate}
            </if>
            <if test="userstate != null and userstate != ''">
                and userstate = #{userstate}
            </if>
            <if test="remark != null and remark != ''">
                and remark = #{remark}
            </if>
        </where>
    </select>

    <!--新增所有列-->
    <insert id="insert" keyProperty="memid" useGeneratedKeys="true">
        insert into hotelmanager.member(usersId, mem_type_Id, memName, phone, memsex, IDCard, email, memInt, regisdate,
                                        userstate, remark)
        values (#{usersid}, #{memTypeId}, #{memname}, #{phone}, #{memsex}, #{idcard}, #{email}, #{memint}, #{regisdate},
                #{userstate}, #{remark})
    </insert>

    <insert id="insertBatch" keyProperty="memid" useGeneratedKeys="true">
        insert into hotelmanager.member(usersId, mem_type_Id, memName, phone, memsex, IDCard, email, memInt, regisdate,
        userstate, remark)
        values
        <foreach collection="entities" item="entity" separator=",">
            (#{entity.usersid}, #{entity.memTypeId}, #{entity.memname}, #{entity.phone}, #{entity.memsex},
            #{entity.idcard}, #{entity.email}, #{entity.memint}, #{entity.regisdate}, #{entity.userstate},
            #{entity.remark})
        </foreach>
    </insert>

    <insert id="insertOrUpdateBatch" keyProperty="memid" useGeneratedKeys="true">
        insert into hotelmanager.member(usersId, mem_type_Id, memName, phone, memsex, IDCard, email, memInt, regisdate,
        userstate, remark)
        values
        <foreach collection="entities" item="entity" separator=",">
            (#{entity.usersid}, #{entity.memTypeId}, #{entity.memname}, #{entity.phone}, #{entity.memsex},
            #{entity.idcard}, #{entity.email}, #{entity.memint}, #{entity.regisdate}, #{entity.userstate},
            #{entity.remark})
        </foreach>
        on duplicate key update
        usersId = values(usersId) , mem_type_Id = values(mem_type_Id) , memName = values(memName) , phone =
        values(phone) , memsex = values(memsex) , IDCard = values(IDCard) , email = values(email) , memInt =
        values(memInt) , regisdate = values(regisdate) , userstate = values(userstate) , remark = values(remark)
    </insert>

    <!--通过主键修改数据-->
    <update id="update">
        update hotelmanager.member
        <set>
            <if test="usersid != null">
                usersId = #{usersid},
            </if>
            <if test="memTypeId != null">
                mem_type_Id = #{memTypeId},
            </if>
            <if test="memname != null and memname != ''">
                memName = #{memname},
            </if>
            <if test="phone != null and phone != ''">
                phone = #{phone},
            </if>
            <if test="memsex != null and memsex != ''">
                memsex = #{memsex},
            </if>
            <if test="idcard != null and idcard != ''">
                IDCard = #{idcard},
            </if>
            <if test="email != null and email != ''">
                email = #{email},
            </if>
            <if test="memint != null">
                memInt = #{memint},
            </if>
            <if test="regisdate != null">
                regisdate = #{regisdate},
            </if>
            <if test="userstate != null and userstate != ''">
                userstate = #{userstate},
            </if>
            <if test="remark != null and remark != ''">
                remark = #{remark},
            </if>
        </set>
        where memId = #{memid}
    </update>

    <!--通过主键删除-->
    <delete id="deleteById">
        delete
        from hotelmanager.member
        where memId = #{memid}
    </delete>

</mapper>

